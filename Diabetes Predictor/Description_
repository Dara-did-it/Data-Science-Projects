This project builds and compares linear regression models (standard, Lasso, and Ridge) to predict the likelihood that a patient will develop diabetes based on patient attributes.

A classification model was also created to predict diabetes. The steps covered include data preprocessing, feature engineering, and training a Logistic Regression model. The model's performance has also been evaluated.

